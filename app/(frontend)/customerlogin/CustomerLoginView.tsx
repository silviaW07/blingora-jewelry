'use client';

/**
 * Alternate page-local login view (page.tsx uses @/frontend/components/CustomerLoginView).
 * Kept form-only so mobile/desktop match the cleaned auth modal (no benefit cards).
 */
export { CustomerLoginView, default } from '@/frontend/components/CustomerLoginView';
